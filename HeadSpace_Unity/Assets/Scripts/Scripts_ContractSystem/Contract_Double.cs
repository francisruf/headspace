using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Contract_Double : Contract
{
    protected override void AddClient(Client client, int index)
    {
        base.AddClient(client, index);
    }
}
